<Window x:Class="DockerRegistryExplorer.View.TextDialogView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DockerRegistryExplorer.View"
        xmlns:viewModel="clr-namespace:DockerRegistryExplorer.ViewModel"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance viewModel:TextDialogViewModel}"
        Title="{Binding Path=Title}" Height="450" Width="800">
    <Grid>
        <TextBox AcceptsReturn="True" AcceptsTab="True" VerticalScrollBarVisibility="Visible" Text="{Binding Path=Text, Mode=OneWay}" FontFamily="Courier New" FontSize="12" IsReadOnly="True">

        </TextBox>
    </Grid>
</Window>
